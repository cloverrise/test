```mermaid
flowchart TB

%% =====================================
%% 旅游线路预定小程序系统架构图（修正版）
%% =====================================

%% =====================================
%% 1. 用户访问层
%% =====================================

subgraph ACCESS["用户访问层"]

    U1[普通用户]

    U2[管理员]

end

%% 普通用户访问前端
U1 --> WX_ENTRY[访问小程序前端]

%% 管理员访问后台管理界面
U2 --> ADMIN_UI[后台管理系统]

%% =====================================
%% 2. 小程序前端
%% =====================================

subgraph FRONTEND["小程序前端 (Uni-app + Vue)"]

    direction TB
    
    P1[首页]
    
    P2[线路列表]
    
    P3[详情页]
    
    P4[订单页]
    
    P5[个人中心]

end

%% =====================================
%% 3. FastAdmin 后端系统
%% =====================================

subgraph BACKEND["FastAdmin 后端系统 (PHP)"]

    direction TB
    
    API[API 接口层<br/>ThinkPHP]

    subgraph MODULES["业务模块（后台功能）"]
        direction TB
        M1[订单管理]
        M2[团队管理]
        M3[产品管理]
        M4[统计分析]
    end

    subgraph SERVICE["服务层"]
        direction TB
        S1[认证服务]
        S2[支付服务]
        S3[短信服务]
    end

    %% API 调用业务模块和服务层
    API --> MODULES
    MODULES --> SERVICE

end

%% =====================================
%% 4. 数据存储层
%% =====================================

subgraph STORAGE["数据存储层"]

    direction TB

    DB[(MySQL)]

    CACHE[(Redis)]

end

%% =====================================
%% 5. 第三方服务
%% =====================================

subgraph EXTERNAL["第三方服务"]

    direction TB

    WX_LOGIN[微信登录API]

    WX_PAY[微信支付API]

    ALI_SMS[阿里云短信API]

end

%% =====================================
%% 系统流程（简化版 - 仅大框关系）
%% =====================================

%% 用户与入口
WX_ENTRY --> FRONTEND
ADMIN_UI --> BACKEND

%% 核心数据流
FRONTEND --> API
MODULES --> STORAGE
SERVICE --> STORAGE

%% 外部依赖
SERVICE --> EXTERNAL

%% =====================================
%% 样式（与参考架构图保持一致）
%% =====================================

classDef user fill:#FFF7E6,stroke:#D98F00,stroke-width:2px,color:#000;
classDef frontend fill:#E6F7FF,stroke:#0099CC,stroke-width:2px,color:#000;
classDef backend fill:#E6FFE6,stroke:#2E8B57,stroke-width:2px,color:#000;
classDef admin_ui fill:#F0E6FF,stroke:#7A4DCC,stroke-width:2px,color:#000;
classDef storage fill:#FFE6E6,stroke:#D9534F,stroke-width:2px,color:#000;
classDef external fill:#F3E5F5,stroke:#9C27B0,stroke-width:2px,color:#000;

class ACCESS user;
class FRONTEND frontend;
class BACKEND backend;
class ADMIN_UI admin_ui;
class STORAGE storage;
class EXTERNAL external;

%% 为业务模块和服务层设置背景色
style MODULES fill:#FFF8E1,stroke:#F39C12,stroke-width:2px;
style SERVICE fill:#E8F5E9,stroke:#4CAF50,stroke-width:2px;